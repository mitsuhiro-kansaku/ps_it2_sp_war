<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.exclusive.QuotReqUploadExclusiveCheckMapper">

    <select id="searchExclusive" parameterType="fw.domain.sp.quot.quotresp.QuotRespUploadItem" 
    		resultType="String">
        SELECT
               S_QUOT_REQ.QUOT_REQ_SEQ
          FROM S_QUOT_REQ
        WHERE
               S_QUOT_REQ.ACC_CD = #{accCd}
               AND S_QUOT_REQ.CUST_CD = #{custCd}
               AND S_QUOT_REQ.APL_NO = #{aplNo}
               AND S_QUOT_REQ.APL_VERSION = #{aplVersion}
        FOR UPDATE NOWAIT
    </select>
    
</mapper>
    