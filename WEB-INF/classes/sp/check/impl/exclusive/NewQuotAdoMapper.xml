<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.exclusive.NewQuotAdoMapper">

    <select id="searchExclusive" resultType="String">
        SELECT
               S_NEW_QUOT_ADO.QUOT_ADO_SEQ
          FROM S_NEW_QUOT_ADO
          WHERE S_NEW_QUOT_ADO.QUOT_ADO_SEQ = #{quotAdoSeq}
          AND S_NEW_QUOT_ADO.LAST_UPD_DATE = #{lastUpdDate}
          FOR UPDATE NOWAIT
    </select>

</mapper>
