<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.UploadShipmentExclusiveCheckMapper">

    <select id="getMaxPartDelivNo" resultType="int">
        SELECT COUNT(ACC_CD)
          FROM S_DELIVERY 
         WHERE ACC_CD = #{accCd}
           AND CUST_CD = #{custCd}
           AND ORDER_NO = #{orderNo}
           AND ORDER_VERSION = #{historyNo}
           AND DELIV_KEY_NO = #{delivKeyNo}
           AND UPD_CNT = '0'
    </select>
</mapper>
