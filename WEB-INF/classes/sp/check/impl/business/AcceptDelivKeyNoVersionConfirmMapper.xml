<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.AcceptDelivKeyNoVersionConfirmMapper">

    <select id="getNumberForUnacknowledged" resultType="Integer">
        SELECT COUNT(S_NEW_RCV.ACC_CD)
          FROM S_NEW_RCV
        <where>
		    S_NEW_RCV.ACC_CD =#{accCd}
           AND S_NEW_RCV.CUST_CD = #{custCd}
		   AND S_NEW_RCV.ORDER_NO = #{orderNo}
		   AND S_NEW_RCV.DELIV_KEY_NO = #{delivKeyNo}
		   AND S_NEW_RCV.DELIV_VERSION &lt; #{delivKeyNoVersion}
		   AND S_NEW_RCV.DPO_FLG = '0'
        </where>   
    </select>    

</mapper>

