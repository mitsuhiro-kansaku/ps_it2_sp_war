<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.UploadOrderFirstDateMapper">

    <select id="getOrderFirthDate" resultType="String">
        SELECT  ORDER_FIRST_DATE
        FROM    S_ORDER
        WHERE   ACC_CD = #{accCd}
        AND     CUST_CD = #{custCd}
        AND     ORDER_NO = #{orderNo}
        AND     ORDER_VERSION = #{orderVersion}
    </select>

</mapper>
