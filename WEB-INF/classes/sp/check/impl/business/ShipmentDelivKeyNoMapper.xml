<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.ShipmentDelivKeyNoMapper">

    <select id="getNumberForUnacknowledged" resultType="int">
       SELECT COUNT(ACC_CD)
         FROM S_DELIVERY
        <where>
            ACC_CD = #{accCd}
            AND CUST_CD =#{custCd}
		   AND ORDER_NO = #{orderNo}
		   <if test="@org.apache.commons.lang.StringUtils@equals(orderVersion,'1')">
		   AND ORDER_VERSION = #{orderVersion}
		   </if>
		   AND DELIV_KEY_NO = #{delivDateKeyNo}
        </where>   
    </select>    
</mapper>
