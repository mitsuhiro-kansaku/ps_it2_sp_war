<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.AcceptDelivKeyNoVersionInfoConfirmMapper">

    <select id="getCountSameVersionAcceptInfo" resultType="Integer">
        SELECT      COUNT(*)
        FROM        S_NEW_RCV
        WHERE       S_NEW_RCV.ACC_CD = #{accCd}
        AND         S_NEW_RCV.CUST_CD = #{custCd}
        AND         S_NEW_RCV.ORDER_NO = #{orderNo}
        AND         S_NEW_RCV.DELIV_KEY_NO = #{delivKeyNo}
        AND         S_NEW_RCV.DELIV_VERSION = #{delivKeyNoVersion}
        AND         S_NEW_RCV.DPO_FLG = '0'
        AND         S_NEW_RCV.DELETE_FLG = '0'
        AND         S_NEW_RCV.CORRECT_CD IN('1', '2')
    </select>    

</mapper>

