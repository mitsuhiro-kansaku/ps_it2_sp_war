<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.QuotReqAplDetailedMapper">

    <select id="getNumberForUnacknowledged" resultType="int">
        SELECT COUNT(1)
          FROM S_QUOT_REQ_DTL
         WHERE
               S_QUOT_REQ_DTL.ACC_CD = #{accCd}
           AND S_QUOT_REQ_DTL.CUST_CD = #{custCd}
           AND S_QUOT_REQ_DTL.APL_NO = #{aplNo}
           AND S_QUOT_REQ_DTL.APL_VERSION = #{aplVersion}
           AND S_QUOT_REQ_DTL.QUOT_REQ_NO = #{quotReqItemNo}
    </select>    

    <select id="getDetailCount" resultType="int">
        SELECT COUNT(1)
          FROM S_QUOT_REQ_DTL
         WHERE
               S_QUOT_REQ_DTL.ACC_CD = #{accCd}
           AND S_QUOT_REQ_DTL.CUST_CD = #{custCd}
           AND S_QUOT_REQ_DTL.APL_NO = #{aplNo}
           AND S_QUOT_REQ_DTL.APL_VERSION = #{aplVersion}
    </select>

</mapper>
