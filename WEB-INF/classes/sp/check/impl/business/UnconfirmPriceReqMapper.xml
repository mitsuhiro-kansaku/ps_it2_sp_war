<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sp.check.impl.business.UnconfirmPriceReqMapper">

    <select id="getNumberForUnacknowledged" resultType="Integer">
        SELECT COUNT(S_NEW_PRICE_REQ.ACC_CD)
          FROM S_NEW_PRICE_REQ
        <where>
               S_NEW_PRICE_REQ.ACC_CD = #{utlUserCd}
           AND S_NEW_PRICE_REQ.CUST_CD = #{buyerCompanyCd}
           AND S_NEW_PRICE_REQ.PRICE_REV_SEQ = #{priceRevSeq}
           AND S_NEW_PRICE_REQ.DPO_FLG = '0'
        </where>   
    </select>    

</mapper>
