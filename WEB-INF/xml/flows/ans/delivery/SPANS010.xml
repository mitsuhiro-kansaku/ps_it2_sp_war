<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/webflow
        http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd"
    parent="base">

    <var name="defaultSearchCondition" class="fw.domain.sp.dlv.dlvresp.DelivRespSearchCondition" />

    <on-start>
        <evaluate expression="SPANS010Service.init()" result="flowScope.searchCondition" />
        <set name="flowScope.isCurrentPageSearch" value="false" />
    </on-start>

    <view-state id="SPANS010" view="ans/delivery/SPANS010" model="searchCondition" >
        <transition on="validate" validate="true">
            <set name="flowScope.isCurrentPageSearch" value="false" />
            <set name="flowScope.isShowList" value="true" />
            <evaluate expression="SPANS010Service.search(searchCondition)" />
        </transition>
        <transition on="search" to="SPANS020" validate="false" />
        <transition on="back" to="home" validate="false" />
    </view-state>

    <subflow-state id="SPANS020" subflow="ans/delivery/SPANS020">
        <input name="searchCondition" value="searchCondition" />
        <input name="isCurrentPageSearch" value="isCurrentPageSearch" />
        <transition on="back" to="SPANS010">
            <set name="flowScope.isCurrentPageSearch" value="true" />
        </transition>
    </subflow-state>

</flow>